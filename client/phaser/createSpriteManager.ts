import { ObjectRegistry } from "./types";

const createSpriteManager = (objectRegistry: ObjectRegistry, id: string) => {
  const createSprite = () => {};

  const getSprite = () => {};

  const hasSprite = () => {};

  return { createSprite, getSprite, hasSprite };
};

export default createSpriteManager;
